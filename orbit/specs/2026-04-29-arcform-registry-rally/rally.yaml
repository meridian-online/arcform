rally: "ArcForm registry capability — assets rename + registry CLI/monorepo/canonical entries"
autonomy: guided
phase: implementing
started: 2026-04-29T00:00:00Z
completed: null
cards:
  - path: orbit/cards/0008-asset-registry.yaml
    status: complete
    spec_dir: orbit/specs/2026-04-29-assets-rename/
    branch: rally/assets-rename
    worktree: main
    parked_constraint: null
    pr: "https://github.com/meridian-online/arcform/pull/13"
  - path: orbit/cards/0022-registry.yaml
    status: implementing
    spec_dir: orbit/specs/2026-04-29-arcform-registry/
    branch: rally/arcform-registry
    worktree: main
    parked_constraint: null
    scope_note: |
      Per author-confirmed scope (rally checkpoint, 2026-04-29): this drive ships
      arcform-side CLI scaffolding only — `src/registry.rs` (or sub-modules),
      `src/cli.rs` extension with the `arc registry {list,show,fetch,run}` tree,
      types, tests with a mocked index. The meridian-online/registry repo
      (registry.yaml index + brewtrend + gnaf canonical entries + CI smoke-test +
      per-entry tags) is sister work in a separate repo and a separate effort.

implementation_order:
  - orbit/cards/0008-asset-registry.yaml
  - orbit/cards/0022-registry.yaml
order_rationale: |
  Serial — shared symbols are vocabulary, not source. Card 0008 renames "asset registry" →
  "asset" across src/, docs, and error messages. Card 0022's new registry CLI surface
  (cli.rs, registry.rs) and end-user docs reference asset-language; sequencing the rename
  first lets card 0022 inherit clean terminology and avoid a parallel-merge churn pass.
  Per design.md Q1 + decision 0011.
notes: |
  Designs predate the rally:
    - Card 0008 design captured in decision 0011 (naming/rename) + design.md notes
    - Card 0022 design captured in interview.md (discovery, 9 Qs) + design.md (design, 4 Qs)
  §4 (decision-pack sub-agents) and §5 (consolidated decision gate) skipped — decisions
  0011–0016 are already accepted and approved in the foundation commit.

  Next: §6a write interview.md for card 0008, §6b consolidated design review, §6c
  disjointness check (expected outcome: serial — captured above), §7b serial implementation.
