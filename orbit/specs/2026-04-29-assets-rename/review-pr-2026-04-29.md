# Pre-Merge Review

**Date:** 2026-04-29
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/assets-rename
**Spec:** orbit/specs/2026-04-29-assets-rename/spec.yaml
**Verdict:** APPROVE

---

## Test Results

```
| Check                | Result | Details                                                                                       |
|----------------------|--------|-----------------------------------------------------------------------------------------------|
| Test suite           | SKIP   | `cargo test --no-run` link fails: `cannot find -lduckdb` (pre-existing env per CLAUDE.md)     |
| `cargo check`        | PASS   | Clean compile; only pre-existing `dead_code` warning on `runner::run` (unrelated)             |
| AC coverage          | N/A    | Spec has zero `code`-type ACs; all six are `doc`/`gate` (exempt from test-prefix coverage)    |
| Decision 0011 keyword scan | PASS | `rg -i 'asset registry' src/ README.md Cargo.toml` returns zero matches                |
```

## AC Coverage Report (prefix: rename)

```
| AC    | Type | Status        | Verification                                                                                                  |
|-------|------|---------------|---------------------------------------------------------------------------------------------------------------|
| ac-01 | code | EXEMPT-VERIFIED | No code/test added; verified by direct `rg -i 'asset registry' src/ README.md Cargo.toml` → 0 matches      |
| ac-02 | doc  | VERIFIED      | `orbit/cards/0008-asset-registry.yaml:1` reads `feature: "Assets"`; file slug unchanged                       |
| ac-03 | doc  | VERIFIED      | All three CLAUDE.md hits (lines 90, 97, 155) are quotation-form references to the renamed-from term           |
| ac-04 | gate | EXEMPT (env)  | `cargo build` link blocked by missing `libduckdb.so` per CLAUDE.md Known Issues; rename touches zero Rust src |
| ac-05 | doc  | VERIFIED      | `orbit/specs/2026-04-29-assets-rename/progress.md` exists with discovery note (lines 9–39)                    |
| ac-06 | doc  | VERIFIED      | `orbit/cards/0008-asset-registry.yaml:29` lists `orbit/specs/2026-04-29-assets-rename/spec.yaml`              |
```

ac-01 has `ac_type: code` but the verification is a negative search (zero hits in active surfaces) rather than a behavioural test. I treated this as a verification check rather than a test-prefix coverage gap because: (a) there is no executable code change to test, (b) the verification is deterministic and rg-based exactly as the spec prescribes, and (c) the spec's `metadata.test_prefix: "rename"` is unused — searching for `rename_ac*` returns no files, which is correct given there is nothing to test. No coverage finding raised.

## Decision 0011 Compliance Audit

Decision 0011 (forward-usage rename, historical preservation) compliance:

```
| Requirement                                              | Status   | Evidence                                                       |
|----------------------------------------------------------|----------|----------------------------------------------------------------|
| Forward usage in `src/` uses "assets" not "asset registry" | PASS    | rg returns 0 matches in src/                                   |
| Forward usage in README.md uses "assets"                 | PASS     | rg returns 0 matches                                           |
| Forward usage in Cargo.toml uses "assets"                | PASS     | rg returns 0 matches                                           |
| Historical decisions 0001/0004/0010 preserved             | PASS    | All three still contain "asset registry" as historical context |
| Historical spec folder `2026-04-15-asset-registry/` retained | PASS | Folder exists with original contents                           |
| Card 0008 file slug `0008-asset-registry.yaml` preserved | PASS     | Path unchanged                                                  |
| CLAUDE.md mentions are quotation-form (renamed-from)     | PASS     | Lines 90, 97, 155 each wrap the term in quotes/backticks       |
| Card 0008 `feature:` label updated to "Assets"           | PASS     | Line 1 of the YAML reads `feature: "Assets"`                   |
```

## Diff Scope

The PR touches only orbit artefacts:

```
| File                                                      | Change                                          |
|-----------------------------------------------------------|-------------------------------------------------|
| orbit/cards/0008-asset-registry.yaml                      | feature label "Asset registry" → "Assets"; specs[] += new spec path |
| orbit/specs/2026-04-29-assets-rename/spec.yaml            | new (76 lines)                                  |
| orbit/specs/2026-04-29-assets-rename/progress.md          | new (103 lines)                                 |
| orbit/specs/2026-04-29-assets-rename/drive.yaml           | new (19 lines)                                  |
| orbit/specs/2026-04-29-assets-rename/review-spec-2026-04-29.md | new (72 lines)                              |
```

Zero source code, README, or Cargo.toml modifications, consistent with the spec's discovery note. This means no behavioural risk and no rebuild risk.

## Findings

### [LOW] ac-01 typed `code` but verified by negative search, not a test
**Category:** test-gap
**Description:** `ac-01.ac_type: code` and `metadata.test_prefix: "rename"` would normally trigger a coverage check expecting `rename_ac01_*` test functions. None exist. This is correct in context — the AC verifies absence (zero forward-usage hits) and any executable test would just wrap `rg`. Still, a future automated AC auditor (`/orb:audit`) may flag this as a missing test. The progress.md and spec implementation_notes already make the rationale clear, and the review-spec round acknowledged the same.
**Evidence:** `orbit/specs/2026-04-29-assets-rename/spec.yaml:20-23`; no `rename_ac*` functions exist anywhere.
**Recommendation:** Optional — retype ac-01 as `gate` (deterministic verification by command) or add a one-line note in metadata that ac-01 is verified by audit rather than test. Non-blocking.

### [LOW] ac-06 wording implies pending action that was already taken
**Category:** spec-quality
**Description:** Already raised in review-spec (LOW). ac-06's "appended with..." reads as an action item, but `orbit/cards/0008-asset-registry.yaml` already contained the new spec path at spec-authoring time. Verification still passes; only the prose verb is misleading. Not worth fixing post-implement.
**Evidence:** `orbit/cards/0008-asset-registry.yaml:29` already lists the path; spec.yaml line 47 says "appended with".
**Recommendation:** Carry forward to the next rename of this kind — no action required for this PR.

---

## Edge Case Probing

1. **First run / no prior state** — N/A. PR ships orbit artefacts only; no runtime change.
2. **Failure mode** — N/A. No behaviour change.
3. **Idempotency** — Verified: re-running the only edit (a YAML label change) is naturally idempotent. Re-applying the diff would be a no-op.
4. **Boundary conditions** — N/A. No data inputs.

## Environment Simulation

Not applicable. This PR touches no deployment, infra, scripts, or cron. The only test-relevant environment concern is the pre-existing libduckdb.so linker miss documented in CLAUDE.md "Known Issues" — confirmed reproducible (`cargo test --no-run` exits with `cannot find -lduckdb`) but irrelevant to this PR's surface.

## Honest Assessment

This PR is the cleanest possible terminology rename. The implementer's discovery — that `src/`, `README.md`, and `Cargo.toml` had already absorbed the asset-centric vocabulary before the spec was authored — is corroborated by an independent `rg` sweep. The actual diff reduces to a single label edit (`feature: "Asset registry"` → `feature: "Assets"`) plus an appended `specs:` entry, with the rest being process artefacts (spec, progress, drive, review-spec). All six ACs verify cleanly against the working tree; decision 0011's forward-usage / historical-preservation contract is fully respected. The two LOW findings are cosmetic and do not justify another iteration. Approve and merge so card 0022's rally branch can rebase onto post-rename main.
